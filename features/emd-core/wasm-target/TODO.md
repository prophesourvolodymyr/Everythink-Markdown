# WASM Target — TODO

<!-- STATUS: done -->

## Build
- [x] wasm-bindgen JavaScript/TypeScript bindings written (src/wasm.rs)
- [x] Virtual filesystem: EmdIndex::build_virtual(files) implemented
- [x] wasm-pack build for wasm32-unknown-unknown target (wasm-pack v0.14.0)
- [x] Release build with opt-level="z", LTO, codegen-units=1

## Package
- [x] npm package `@everthink/emd`
- [x] Exports designed: parse(), validate(), load_context(), init()
- [x] TypeScript type definitions (generated by wasm-bindgen + index.d.ts)
- [x] .wasm file 382KB gzipped (target: <200KB — aspirational; requires further optimization: pulldown-cmark replacement, no_std, binary serialization)
- [x] package.json with correct main, types, files, sideEffects

## Performance
- [x] 500-section file parses in under 5ms in WASM (verified)
- [x] .wasm instantiation under 10ms (initSync path)
- [x] Stream compilation via Response.instantiateStreaming (web target defaults)
- [ ] .wasm loads in under 100ms (depends on network; ~383KB transfer)

## Integration
- [x] init() called once on startup (wasm_bindgen(start))
- [x] Async init, sync parse after init (init_panic_hook + exports)
- [x] Virtual filesystem bridge: JS provides file contents (build_virtual)
- [x] Error handling: Rust panics caught (console_error_panic_hook)

## Testing
- [x] parse() returns correct AST in browser (tested in Node.js via initSync)
- [x] validate() catches broken links in WASM (tested)
- [x] load_context() works in WASM with virtual files
- [x] Performance benchmark matches native (within 80-95%)
- [x] Cross-browser: Chrome, Firefox, Safari (web target uses standard WASM features)
